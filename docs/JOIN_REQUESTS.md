# Join Request Review

Join requests are created via `/request-join/` and stored as `JoinRequest` records.

Superusers can review and update request status at `/super/join-requests/`.
This view only updates the status (approve/reject); it does not provision tenants or users.
